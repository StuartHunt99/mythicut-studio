# MythiCut Studio agent instructions

- Read `PROJECT_CONTEXT.md` before design, implementation, diagnosis, or release work. Follow its source-of-truth map instead of assuming every plan item is implemented.
- Read `CONTEXT.md` when interpreting product language. Use its canonical terms, and update it when the domain gains or changes a term.
- Update `PROJECT_CONTEXT.md` in the same change whenever product behavior, storage or portability, external prerequisites, verified capabilities, open risks, or the documentation map changes.
- Treat `artifacts/`, local models, credentials, catalogs, recordings, and generated analysis as external user data unless a tracked fixture explicitly proves otherwise.
- Preserve user decisions and original media. Automated suggestions remain provisional until the relevant human-review action accepts them.
